# Binaries
/go-you
/server
*.exe
*.out

# Local env / secrets
# NOTE: deploy/secret.yaml is intentionally COMMITTED for the POC (contains live
# values). Move to a secret store and re-ignore this before any real use.
.env

# OS / editor
.DS_Store
.idea/
*.swp

# Go
vendor/
